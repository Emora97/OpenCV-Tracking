import cv2
from cv2 import aruco

dictionary = aruco.getPredefinedDictionary(aruco.DICT_6X6_50)

for i in range(4):
    marker = aruco.generateImageMarker(dictionary, i, 300)

    cv2.imwrite(f"6xmarker_{i}.png", marker)
